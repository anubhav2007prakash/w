# ML Model Evaluation

Cross-validation uses time-series splits (Walk-Forward Validation) to avoid lookahead bias.
Evaluation metrics track Mean Absolute Error (MAE), Root Mean Squared Error (RMSE), and Normalized RMSE.
