# Safety & Fallbacks

Deterministic pattern matching handles weather queries even when external LLM endpoints experience outages or latency spikes.
Emergency warnings are pulled directly from verified IMD feeds without synthetic alteration.
