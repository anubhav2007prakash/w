# Database Relationships

- Each `favourite` belongs to a `location_name`.
- Each `crowd_report` references a `location_name` and optional `reporter_name`.
- Alerts are grouped by geographic district or basin.
