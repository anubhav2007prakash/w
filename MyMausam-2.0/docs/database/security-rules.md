# Security Rules

Strict role-based access control (RBAC):
- Read access is globally permitted for weather observation data.
- Write access for emergency alerts requires the `admin` or `disaster_official` claim.
