# Weather API

### `GET /api/weather/current?location={location}`
Returns real-time temperature, humidity, wind, UV index, and AQI for a location.

### `GET /api/weather/hourly?location={location}`
Returns 24-hour hourly step forecast.

### `GET /api/weather/forecast?location={location}`
Returns 7-day daily forecast.
