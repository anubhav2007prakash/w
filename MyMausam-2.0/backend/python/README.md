# MyMausam 2.0 - Python FastAPI Backend

High performance meteorological telemetry, radar reflectivity, cyclone trackers, and ML inference service.

## Local Setup
```bash
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

Open Swagger UI at `http://127.0.0.1:8000/docs`.
