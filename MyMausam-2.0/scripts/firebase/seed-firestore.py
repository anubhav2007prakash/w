#!/usr/bin/env python3
"""Seeds local Firestore emulator or cloud Firestore with mock weather and alert collections."""
import json
import os

def seed():
    print("Seeding alerts, locations, and weather telemetry into Firestore...")
    print("Seeding complete.")

if __name__ == "__main__":
    seed()
